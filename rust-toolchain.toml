# Rust toolchain configuration
# This ensures consistent Rust version across all environments (local + CI)
# Edition 2024 support requires Rust 1.85.0 or later (stable provides this)

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
